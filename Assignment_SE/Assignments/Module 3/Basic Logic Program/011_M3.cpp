/*(11)find Circumference of Square formula:C=4*a
NOTE:Question is WRONG, Square don't have circumferences it has perimeter,Formula: P=4*a  
*/

#include <stdio.h>

int main() {
    int Perimeter,a;
    
    printf("enter the value of a:");
    scanf("%d",&a);
    
    //Perimeter of Square=4*a
    Perimeter=4*a;
    printf("\nThe Perimeter of Square is %d",Perimeter);
    
    

    return 0;
}
